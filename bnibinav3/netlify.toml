# Build when this folder is the Netlify site root (or Base directory = this folder).
[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
